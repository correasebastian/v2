(function() {
    'use strict';
    angular.module('blocks.zumero', [])

/* .run(runZumero)

 runZumero.$inject=['$ionicPlatform','zumeroService'];

 function runZumero($ionicPlatform,zumeroService) {
	  $ionicPlatform.ready(function() {
	  	z=zumeroService;
	  	zumeroService.setZumero('zzdbfile');
	  	zumeroService.zync(1);	  
	  });
	}*/

})();
